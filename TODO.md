# 小动物心理陪伴 Demo TODO

这个文件记录当前 demo 已经完成的能力、还没做的事情，以及后续可以逐步升级的方向。`ROADMAP.md` 偏长期规划；这里偏日常开发清单。

## 已完成

- Web UI：本地浏览器访问、手机局域网访问、移动端适配。
- 基础对话：支持 DeepSeek / fake model 切换。
- 长期存储：SQLite 保存 sessions、messages、journals、memories。
- 会话总结：结束会话后生成 journal，并抽取长期记忆。
- 记忆管理：支持记忆分类、小类、关键词、合并、更新、冲突处理。
- 数据看板：可查看 sessions、messages、journals、memories、knowledge、content、mood。
- 心情视图：基于 journal 展示心情轨迹、日历和周报原型。
- 知识卡：可在回复中展示本轮参考知识卡，并点开查看相关内容卡。
- 多角色：支持绵绵羊、石石龟、墨墨鸦、忧忧兔、闪闪蝶、敢敢虎。
- 继续会话：可以从历史 session 继续对话。
- 群聊自动 v0：通过关键词规则自动选择一只小动物回复。

## 近期 TODO

- 让已结束 session 继续对话时有更清晰的状态处理：
  - 方案 A：允许继续追加。
  - 方案 B：从旧 session 分支出新 session。
- 给每条 assistant 消息显示“为什么选这个角色”的简短说明。
- 在数据看板里增加角色维度统计：
  - 每个角色回复次数。
  - 哪些情绪更常触发哪个角色。
- 优化群聊自动的 UI 表达：
  - 显示“小动物们商量了一下，由 XX 来回应”。
  - 避免系统提示过多打断聊天流。
- 更新截图，保证 README 展示的是当前六角色 UI。

## 群聊自动 v1：大模型选角

当前群聊自动只是关键词检测，成本低、速度快，但理解能力有限。下一版可以改成 LLM 路由器。

目标流程：

1. 用户输入文本。
2. 调用大模型做情绪和意图判断。
3. 大模型输出结构化 JSON：
   - `dominant_emotion`
   - `emotion_intensity`
   - `support_need`
   - `recommended_character_id`
   - `reason`
4. 后端根据推荐角色生成最终回复。
5. 前端展示“本轮由谁回应”和简短理由。

注意事项：

- 这会多一次模型调用，成本和延迟都会增加。
- 需要稳定的 JSON 输出和失败兜底。
- 如果路由器失败，应回退到当前关键词规则。
- 选角 prompt 需要单独版本管理和测试用例。

## 群聊自动 v2：多阶段 Agent 编排

更高级的版本可以参考 Claude Code / OpenClaw 的 agent 思路，把一次回复拆成多个步骤。

候选流程：

1. `EmotionAnalyzer`：识别情绪、强度、身体状态、关系触发。
2. `RoleRouter`：判断适合哪只小动物，或是否需要多只共同回应。
3. `KnowledgeRetriever`：检索心理学知识卡、内容卡、练习卡。
4. `ResponsePlanner`：规划回复结构和总字数。
5. `AnimalResponder`：按角色生成回复。
6. `SafetyGuard`：检查风险、边界和依赖性语言。
7. `MemoryWriter`：决定是否写入长期记忆。

设计原则：

- 不追求“每次都多 agent”，只在必要时多步。
- 控制 token 和延迟，避免一次普通对话变得太重。
- 每一步都要有日志和可视化反馈，让开发者知道发生了什么。
- 多角色回复要控制总字数，不能简单把多个完整回复拼起来。

## 多角色群聊 v3：多只小动物共同回应

未来可以让 1-3 只小动物一起出现，但必须控制信息密度。

可能模式：

- 主回复：一只小动物负责完整回应。
- 共情补充：一只小动物只说一句情绪陪伴。
- 行动补充：一只小动物只给一个小下一步。
- 表情/动作：一只小动物只提供短动作，例如“忧忧兔轻轻点头”。

约束：

- 总字数最多比现在单角色回复多一点，不能翻倍。
- 默认 1 只，必要时 2-3 只。
- 不要让角色互相抢话。
- 不要让用户感觉被很多声音包围。

## 心理学内容与检索

- 扩展知识卡来源：
  - 创伤知情原则。
  - CPTSD / 《不原谅也没关系》相关概念。
  - 非暴力沟通。
  - 高敏感人群。
  - IFS / parts work。
  - ACT、CBT、DBT。
- 给知识卡增加更细的适用条件和禁用条件。
- 让知识卡、内容卡、练习卡之间形成更明确的关联。
- 后续尝试检索增强：
  - 先用关键词和标签。
  - 再考虑 embedding / 向量检索。

## 记忆系统后续

- 给每条记忆增加“角色是否用过它”的追踪。
- 增加记忆编辑和删除功能。
- 支持手动标记记忆：
  - 重要。
  - 过期。
  - 不准确。
  - 不想再被提起。
- 增加更清晰的记忆合并可视化。
- 让 memory retrieval 不只按类别和重要度，也考虑当前情绪、当前角色、近期主题。

## 产品化准备

- PWA：手机添加到主屏幕。
- 云端部署：HTTPS、域名、基础鉴权。
- 隐私策略：明确本地/云端数据保存方式。
- API key 策略：
  - 开源版本让用户自己配置 key。
  - 线上体验需要后端代理、额度限制和风控。
- 安全边界：
  - 危机干预提示。
  - 不替代心理治疗。
  - 不提供诊断和处方建议。

## 工程质量

- 给关键流程加测试：
  - 选角规则。
  - 记忆合并。
  - session 继续对话。
  - journal mood 输出。
- 增加 prompt 版本管理。
- 增加 trace 页面：
  - 本轮用了哪个角色。
  - 用了哪些知识卡。
  - 检索了哪些记忆。
  - 是否触发安全规则。
- 把当前单文件 `app/web.py` 在合适时机拆分，但不要过早重构。
